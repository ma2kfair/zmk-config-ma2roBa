# マウス機能を有効化（&mmv, &msc, &mkp を使うために必須）
CONFIG_ZMK_MOUSE=y

# 接続の安定性を高める（任意ですが推奨）
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# センサー（エンコーダー）を有効にする
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# 反応が悪い、または全く動かない場合に数値を調整（1, 2, 4のいずれかが多い）
CONFIG_EC11_RESOLUTION=4
